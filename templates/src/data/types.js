export type WindowSize = {
  width: number,
  height: number
};

export type Layout = {
  small: Boolean,
  medium: Boolean,
  large: Boolean
};

export type LinkType = {
  text: string,
  path: string
};
